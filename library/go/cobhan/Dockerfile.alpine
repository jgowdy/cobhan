FROM docker.io/alpine:3

RUN apk add gcc-go build-base git

COPY * /cobhan/

WORKDIR /cobhan

RUN mkdir -p /output

RUN LD_RUN_PATH=\$ORIGIN CGO_ENABLED=1 GOOS=linux go build -compiler=gccgo -buildmode=archive -o /output/cobhan.a
