FROM docker.io/buster

#RUN apk add gcc-go build-base git

COPY . /cobhan/

WORKDIR /cobhan

RUN /cobhan/build.sh

#RUN LD_RUN_PATH=\$ORIGIN CGO_ENABLED=1 GOOS=linux go build -v -compiler=gccgo -buildmode=archive -o /output/cobhan.a
