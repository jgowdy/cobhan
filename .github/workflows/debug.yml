name: Debug

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  CARGO_TERM_COLOR: always
    
jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: true
      matrix:
        os: [ubuntu-latest, macOS-latest]
    steps:
    - name: Install gcc-go
      run: ./install-gccgo.sh
      working-directory: library/go/libplugtest
    - uses: actions/checkout@v2      
    - name: Build Go
      run: ./build-debug.sh
      working-directory: library/go/libplugtest
    - name: Build Rust
      run: ./build-debug.sh
      working-directory: library/rust/libplugtest
